{{#each postagem}}
<div class="card">
    <div class="card-body">
        <h1>{{titulo}}</h1>
        <br>
        <h4>{{categoria.nome}}</h4>
        <hr>
        {{conteudo}}

    </div>
</div>
{{/each}}