<script>
    window.onload = function () {
        var cadastroDiv = document.getElementById('cadastro');
        if (cadastroDiv) {
            cadastroDiv.parentNode.removeChild(cadastroDiv);
        }
    };
</script>

{{#each erros}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}
<div class="card mt-4">
    <div class="card-body">
        <form action="" method="post">
            <label for="">Email:</label>
            <input type="email" name="email" id="" class="form-control mt-2" placeholder="Digite seu email" required>
            <br>
            <label for="">Senha:</label>
            <input type="password" name="senha" id="" class="form-control mt2" placeholder="Digite a senha" required>
            <button type="submit" class="btn btn-success mt-4">Entrar</button>
            <a href="/usuario/registro"><button type="button" class="btn btn-primary mt-4">Cadastra-se</button></a>
             <button type="button" class="btn link-danger link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover position-relative mt-4"
                    style="--bs-link-hover-color-rgb: 25, 135, 84; " data-bs-toggle="modal" data-bs-target="#exampleModal">
                Esqueceu a senha?
            </button>
            
            
        </form>
       

            <!-- Modal -->
            <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel"
                aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="card-body ">
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                            <form action="/usuario/emailRecSenha" method="post">
                                <h4>Insira o email de recuperação:</h4>
                                <input type="email" name="email" id="" class="form-control mt-4"
                                    placeholder="Email de recuperação">
                                <button type="submit" class="btn btn-success mt-4">Recuperar Senha</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        <div class="btn-group" role="group" aria-label="Third group">

    </div>
</div>